INSTALL


sudo pacman -S zsh-autosuggestions zsh-syntax-highlighting

sudo pacman -S zsh

After install in your terminal run "zsh"
and press 0.

my st and dwm config are already set to zsh as there defult shell.

